<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-primary">
      <div class="container-fluid">
        <%= link_to "Back to Restaurants", restaurants_path, class: "navbar-brand text-light fw-bold" %>
      </div>
    </nav>

    <div class="container-fluid ms-3">
        <h1 class="mt-3"><%= @restaurant.name %></h1>
        <span class="m-0"><%= "#{@restaurant.opening_time.to_fs(:time)} - #{@restaurant.closing_time.to_fs(:time)}" %></span>
        <span>
            <%= @restaurant.opening_time.to_fs(:time) < Time.now.to_fs(:time) and
                @restaurant.closing_time.to_fs(:time) > Time.now.to_fs(:time) ?
                "Open" : "Closed" %>
        </span>
        <p><%= @restaurant.address.to_s %></p>
        <p><%= "#{@restaurant.cuisine.capitalize} cuisine" %></p>
    </div>
        
    <% for item in @menu %>
        <div class="card mt-2 ms-3 me-3">
            <div class="card-header d-flex align-items-center justify-content-between">
                <h5><%= "#{item.name} - #{item.price} Kč" %></h5>
                <%= button_to "Add to Cart", add_cart_path(item.id), method: :post, class: "btn btn-primary" %>
            </div>
            <div class="card-body">
                <%= item.description %>
            </div>
        </div>
    <% end %>
</body>
</html>